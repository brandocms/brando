import { gsap, Dom } from '@brandocms/jupiter'

export default class Presence {
  constructor (app) {
    this.app = app
    console.log('Setup Presence')
    this.setupListener()
  }

  setupListener () {    
    
  }
}