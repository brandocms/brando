query ImageCategories {
  imageCategories {
    id
    name
    slug

    cfg {
      uploadPath
    }

    creator {
      id
      name
      avatar {
        thumb: url(size: "xlarge")
      }
    }

    imageSeriesCount

    updatedAt
    deletedAt
  }
}
