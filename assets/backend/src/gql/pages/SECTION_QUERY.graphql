query Fragment ($matches: FragmentMatches, $status: String, $revision: ID) {
  fragment (matches: $matches, status: $status, revision: $revision) {
    id
    title
    parentKey
    pageId
    key
    language
    wrapper
    data
    creator {
      id
      name
    }
    insertedAt
    deletedAt
  }
}
