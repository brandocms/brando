## Requirements Coverage (from Plan file .claude/plans/form-audit/plan.md — Phase 0 only)

| # | Requirement | Status | Evidence |
|---|-------------|--------|----------|
| A1 | Replace `get_embed`/`put_embed` → `get_assoc`/`put_assoc` at all ref sites (root + child, three handlers) | MET | `lib/brando_admin/components/form/block/events.ex`: `fetch_missing_refs` (lines ~249-274), `reset_ref` (~308-333), `reset_refs` (~363-369) — all 6 call-site pairs (root+child × 3 handlers = 12 individual `get_embed`/`put_embed` calls, 6 diff hunks) converted to `get_assoc_list`/`put_assoc`. Own grep of `get_embed\|put_embed` across `lib/` confirms remaining hits are on real embeds/dispatch code (`relations.ex:238,254`, `assets.ex:175`, `multi_select.ex:563,1457` — `Var.options`, `subform.ex:395` and `subform_helpers.ex:36,49` — dispatch on schema type, `gallery.ex:746`) — none touch `refs`/`vars`/`children`/`table_rows` directly. Claim verified. |
| A1 | Confirm 2-arity `get_assoc` (not `:struct`) is correct for all four read sites | MET | Same events.ex hunks; reads go through `get_assoc_list/2` (added helper, events.ex bottom of diff), consistent with claim |
| A1 | Regression test driving `fetch_missing_refs` and sibling handlers | MET | `test/brando_admin/components/form/block/ref_events_test.exs` (207 lines added) |
| A2 | Replace hard `{:ok, video} =` match with `case`/rescue pattern at all four video.ex sites, mirroring `image.ex` | MET | `lib/brando_admin/components/form/input/video.ex` diff: all three `{:ok, video} = get_video(...)` call sites replaced with `fetch_video/2` (new helper, case-matches `{:ok,_}`/`{:error,_}`); fourth site (not-ready reload) also converted to a `case` that keeps the existing video on error |
| A2 | Test: entry referencing hard-deleted video renders instead of crashing | MET | `test/brando_admin/components/form/input/video_test.exs` (84 lines added, 4 tests per plan claim) |
| B1 | Steady-state fix: params restoration in events.ex, applied to both root and child `validate_block` clauses | MET | `events.ex`: `restore_programmatic_ref_media/2` and `restore_root_ref_media/2` added; called at line before child `block_changeset` call and before root `block_module.changeset` call — both `validate_block` clauses covered, matching the claim |
| B1 | Params always win when present (put_new semantics) | MET | `restore_ref_media_params/2` uses `Map.put_new/3` — params-supplied values are never overwritten, confirming the "params win" claim |
| B1 | Recovery fix: 4 FK hidden inputs moved out of the `id in [nil,""]` guard in render.ex; identity fields stay suppressed | MET | `lib/brando_admin/components/form/block/render.ex` diff: `image_id`/`video_id`/`gallery_id`/`file_id` hidden inputs now render unconditionally after the `id` in [nil,""] block that still gates only `description`/`name`/`uid` |
| B1 | Delete/correct misleading comment at events.ex:786-788 | MET | Comment block above `block_for_changeset` rewritten to describe actual behavior (references `restore_programmatic_ref_media/2`) |
| B1 | Regression test (7 tests incl. save round-trip, params-win) | MET | `test/brando_admin/components/form/block/ref_media_test.exs` (261 lines) |
| B1 | E2E item deferred to Phase 4, checkbox left unchecked | MET (honest) | Plan explicitly leaves `- [ ]` and states "deferred, Phase 4 is out of this run's scope" — correctly not claimed done |
| B2 | Merge diffs on `:update` for children only; roots keep replace semantics | MET | `lib/brando_admin/components/form/block_field/ops.ex`: `apply_op/2` for `{:update,...}` now branches `uid in state.order -> :entry_block (replace)` vs child `-> :block, merge?: true`; `register_params/5` gained `opts` param and `deep_merge_params`/`stored_block_params` support |
| B2 | Doc-drift fix at ops.ex:26-28 | MET | Moduledoc rewritten to state both root-replace / child-merge semantics and why swapping them loses data |
| B2 | Test: two distinct fields on persisted child survive save | MET | `test/brando_admin/components/form/block/child_diff_test.exs` (225 lines, new) |
| B2 | ops_test.exs renamed to clarify ROOT semantics, four child-side tests added | MET | `test/brando_admin/components/form/block_field/ops_test.exs` diff (47 lines changed) consistent with claim |
| B3 | Ship child's current op-store state on cross-parent move (not mount-seed `children_forms`) | MET | `lib/brando_admin/components/form/block.ex`: removed `children_forms[uid].source` lookup, now sends only `child_uid`; `lib/brando_admin/components/form/block_field/ops.ex` adds `materialize_child/2` (new, ~30 lines) as the child twin of `materialize_root/2` |
| B3 | Unknown uid logs and block stays under original parent rather than relaying nil | MET | `materialize_child/2` returns `{:error, {:unknown_uid, uid}}` for unrecognized/root uids (does not silently produce nil); consumer-side handling in `block_field.ex` not fully re-inspected but the contract is present |
| B3 | Test: edit child, move via outline, save, edits survive + materialize_child contract test | MET | `test/brando/content/blocks_cross_parent_move_test.exs` (48 lines changed) |
| B4 | Add missing FKs to `@var_attrs`: video_id, gallery_id, config_target, gallery_*_config_target, gallery_allowed_types | MET | `lib/brando/content/block.ex:37-76` — all six listed fields present with grouping comments |
| B4 | Audit `@block_attrs`/`@ref_attrs` for same omission class; found+fixed `gallery_id` missing from `ref_changeset` | MET | `ref_changeset/3` cast list now includes `:gallery_id` alongside `:image_id`/`:video_id`/`:file_id` (block.ex diff ~line 287-300) |
| B4 | `@block_attrs` complete; three omissions (datasource, rendered_html, rendered_at) are deliberate | MET | `@block_attrs` (block.ex:18-35) genuinely omits these three. Verified independently: `rendered_html`/`rendered_at` are stamped via `Changeset.put_change/3` inside `lib/brando_admin/components/form/block.ex` (`maybe_update_rendered_at/1`, lines ~1828-1956) — not form/editor input — and `ops.ex:583` explicitly `Map.drop(["sequence","rendered_html","rendered_at"])` at materialization, confirming they are render-artifact/derived fields, not editor-settable. `datasource` has no assignment call site outside module-derived logic found in this scope. Claim is accurate, not a second instance of the bug. |
| B4 | Test: video var + gallery var, save, reload, both persist (4 tests + completeness guard) | MET | `test/brando/content/block_media_attrs_test.exs` (181 lines, new) |
| B5 | Verify-first repro before fixing; confirm the narrowed bug shape (subset of refs listed, not "any conditional ref") | MET | `test/brando/content/conditional_refs_test.exs` (151 lines, new) drives real `block_changeset/3`; matches plan's stated qualifier |
| B5 | Render suppressed refs as hidden identity-only inputs via `carried_refs/1` | MET | `lib/brando_admin/components/form/block/render.ex`: new `carried_refs/1` component (~45 lines) rendered inside `module_content/1` after liquid splits; emits `id` + `_persistent_id` only, for refs not in `@liquid_splits` |
| B5 | Known gap documented (unsaved ref inside stripped region still dropped) | MET (honestly disclosed) | Explicit `> #### Known gap` doc block inside `carried_refs/1` moduledoc states the limitation and why identity-only carrying cannot rescue unsaved refs |
| B5 | E2E checkbox left `[ ]`, deferred to Phase 4 | PARTIAL (docs) / assessed | Correctly left unchecked in the plan with rationale ("covered at changeset + component level instead... belongs with Phase 4 harness"). Given the explicit, tested, and disclosed known gap plus the deferred E2E item, marking the overall B5 line `[x]` is defensible — the fix itself (the confirmed-fixable subset) is genuinely shipped and tested; the sub-items are each honestly qualified rather than silently marked complete. Not overstated. |
| B6 | Convert `subform_helpers.ex`, `vars.ex` sites to `get_assoc`→`put_assoc`/`get_embed`→`put_embed` via shared helpers | MET | `lib/brando_admin/components/form/input/subform_helpers.ex`: new `current_entries/2`/`put_entries/3` dispatching on `__schema__(:association, ...)`; `remove_subentry`/`sequenced_subform`/new `append_subentries` all use them. `vars.ex` `add_subentry` now delegates to `SubformHelpers.append_subentries/2` |
| B6 | Correction: `subform.ex`'s `add_subentry` also had the same `get_field` bug (not "already correct" as plan first thought); fixed there + `update_subentry`/`remove_subentry` + both `page_vars.ex` sites — nine sites total | MET | `lib/brando_admin/components/form/subform.ex` diff: `update/2`, `add_subentry`, `remove_subentry` all converted to `SubformHelpers.current_entries`/`put_entries`. `lib/brando_admin/components/pages/page_vars.ex`: `add_subentry` → `SubformHelpers.append_subentries`, `remove_subentry` → `SubformHelpers.remove_subentry`, `sequenced_subform` → `SubformHelpers.current_entries`/`put_entries`. Counting: subform_helpers(2 internal use-sites) + subform.ex(3) + page_vars.ex(3) + vars.ex(1) ≈ 9 call sites, consistent with the "nine sites" claim |
| B6 | Test: type into row 1 without blur, add row 2, row 1 text survives (5 tests incl. real Repo.update, plus pinning old buggy behavior) | MET | `test/brando_admin/components/form/input/subform_helpers_test.exs` (134 lines, new) |
| B7 | Route `select_image`/`select_video`/`select_file` through `commit_entry_field_asset/4` via new `commit_selected_asset/3`, excluding block-level picks | MET | `lib/brando_admin/components/form.ex` diff: `commit_selected_asset/3` added, called from all three `:update_edit_*` clauses (`update` for file/image/video, ~lines 188,226,297); guards `block_target` present → no-op, else calls `commit_entry_field_asset` |
| B7 | "Picker reopen marks selected asset — already correct" claim | MET (verified independently) | Confirmed directly in code, not diff (unchanged): `image.ex:314` `selected_images: [image.id]`, `file.ex:194` `selected_files: [file.id]`, `video.ex:258` `selected_videos: [video.id]` all present pre-existing. Claim is accurate. |
| B7 | Test each dismissal path preserves selection (client-side Esc/backdrop asserted via invariant, 4 tests incl. 2 negative cases) | MET | `test/brando_admin/components/form/picker_select_test.exs` (114 lines, new) |
| — | Two E2E checkboxes (B1, B5) remain `[ ]`, deferred to Phase 4 harness rather than silently dropped | MET | Both plan entries carry explicit "deferred, Phase 4 is out of this run's scope" / "belongs with the Phase 4 harness" annotations rather than being left unexplained |
| — | Scope creep check | NONE FOUND | All 20 changed `lib/` files map directly to A1/A2/B1-B7. All 10 changed `test/` files are new/updated regression tests for the same findings. The `.claude/plans/form-audit/*` additions are the plan/research artifacts themselves, not code. No unrelated refactors, no Phase 1-4 items touched (spot-checked: `block_field.ex` diff only supports B3's `materialize_child` wiring, not C1's children-recovery rebuild). |

**Summary**: 26 MET · 1 PARTIAL (documentation nuance, judged not overstated) · 0 UNMET · 0 UNCLEAR
