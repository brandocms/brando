## Requirements Coverage (from Plan file .claude/plans/form-audit/phase-9-plan.md)

| # | Requirement | Status | Evidence |
|---|---|---|---|
| 1 | 9A-1: retire nine non-task checkboxes, convert to status markers matching annotations | MET | `plan.md:212,315,435` `[RESOLVED ELSEWHERE]`; `:696,1084` `[REVERTED]`; `:960,1032` `[REJECTED AS WRITTEN]`; `:1170` `[DECISION, not a task]` — all 7 marker types present, checkbox+annotation no longer disagree |
| 2 | 9A-2: carry Phase 4's note up to the three RESOLVED-ELSEWHERE boxes | MET | `plan.md:212,315,435` each begin with the E2E finding text; spot-checked `:212` reads as a pointer-style annotation (not independently re-verified against Phase 4 block wording, but present at all three sites) |
| 3 | 9A-3: correct `form.ex` line count, note it's measured with date | MET | `plan.md:1111` heading "`form.ex` is **6211 lines** (measured 2026-08-07 after 9C; was 6565 before it, 6257 when this was written)"; actual file `wc -l lib/brando_admin/components/form.ex` = 6210 (off by one from stated 6211, immaterial) |
| 4 | 9A-4: add `## START HERE` block above executive summary | MET | `plan.md:30` `## START HERE — audit status (updated 2026-08-07)`, precedes `## Executive summary` at `:98` |
| 5 | 9B: unify credential handling — Cloudflare raises like Mux/Bunny | MET | `lib/brando/videos/uploaders/cloudflare.ex:283-284` `if not present?(...) or not present?(...) do raise ...`; matches `mux.ex:545`, `bunny.ex:403` raise sites — all three citations still resolve correctly |
| 6 | 9B: `delete_remote/1` and form-upload paths checked, not assumed, before the change | UNCLEAR | asserted in plan prose (`phase-9-plan.md:136-142`); form.ex rescue behavior around `initiate_provider_upload/5` not independently line-verified in this pass |
| 7 | 9B: one test per provider raising, plus the empty-string difference pinned | MET | `test/brando/videos/provider_client_test.exs:304` `describe "missing credentials"`, tests at `:305` (Mux raises), `:315` (Bunny raises), `:325` (Cloudflare raises), `:340` (Cloudflare rejects empty-string) — 4 tests as claimed |
| 8 | 9B: CHANGELOG entry under Breaking, names old shape/before-after/paths affected/empty-string diff | MET | `CHANGELOG.md:5` `#### Breaking`, `:8-9` old shape, `:26-27` what to change, `:32,37` paths affected, `:42` empty-string difference noted |
| 9 | 9C-1: extract `Form.VideoDrawer` as markup-only component (`:component`, `render/1`), all `update/2`/`handle_event/3` clauses stay in `form.ex` | MET | `lib/brando_admin/components/form/video_drawer.ex:1-27` moduledoc states markup-only rationale; `form.ex:3276-3789` retains all cited handlers (`reset_video_field`, `reset_video_thumbnail`, `parse_video_url`, `extract_thumbnail`, `validate_video`×2, `save_video`×2, `save_video_authorized`×2); call site `form.ex:2069` `<VideoDrawer.render` |
| 10 | 9C-1: "markup only, seam claim checked and refuted" — not merely asserted | MET | `video_drawer.ex:14-20` documents the specific refutation (inbound `send_update`, outbound direct assignment via `save_video_authorized` writing parent `:form`/`:entry`); `form.ex:5786` comment cross-references `VideoDrawer.render/1` for the sibling coupling — evidence is structural (handlers left in place), not just a claim |
| 11 | 9C-1: `"extract_thumbnail"` confirmed to belong to video drawer, not shared with image editor | UNCLEAR | claimed at `phase-9-plan.md:221-223` ("only caller `video_thumbnail_section/1`, rendered only from `video_drawer/1`") — plausible given handler stayed in `form.ex` alongside other video handlers, but not independently re-verified via grep for `video_thumbnail_section` call sites in this pass |
| 12 | 9C-2: E2E gate green (107/0) then RED (dropped `phx-submit` reddens `projects.spec.js:290`) | UNCLEAR | claim only — no artifact in diff to cite; E2E run not re-executed by this reviewer per scope (verification-runner agent owns gates) |
| 13 | 9C-3: cost measured — 354 lines moved, table with mix test/credo/E2E figures | MET (arithmetic) / UNCLEAR (test gates) | `wc -l form.ex` = 6210, `video_drawer.ex` = 390 — consistent with "6565→6211, new file 390 lines (353 moved + header)" claimed at `phase-9-plan.md:245-246`; gate numbers (mix test 1291+135, credo 284, E2E 107/0) are unverifiable claims without recorded artifacts in the diff |
| 14 | Section G stale line citations fixed (`:6257` past EOF, `handle_event:3549-4069` wrong range) | MET | `plan.md:1111` heading corrected to 6211; `plan.md:1149` explicitly states the old range "**missed** save_video/save_video_authorized and **swallowed**" unrelated handlers; `plan.md:1169` states "`:6257` is past its end" — stale citations are called out and corrected in text, not silently left |
| 15 | 9D: `:418` / C4 — verify static "no push_patch path exists" claim, correct if false | MET | `phase-9-plan.md:280-289` documents the claim was WRONG — cites `form.ex:2977` `push_patch(to: update_url)` on save-and-continue; confirmed live at `form.ex:2977`; `plan.md:65` records "CLOSED 2026-08-07 (9D) — not a leak, and the reason the audit had been giving for it was wrong about its fact" — false claim corrected in plan text, not just deleted |
| 16 | 9D: C4 pinned by an actual E2E test, not just prose | MET | `e2e/e2e/playwright/tests/blocks/block-recovery.spec.js:215` `test('the recovery key follows the entry across save-and-continue', ...)` — new test present (file has 5 tests total: `:34,81,128,160,215`) |
| 17 | 9D: RED measured — replacing `data-entry-id={@entry.id}` with `nil` reddens exactly the new test | UNCLEAR | claim only (`phase-9-plan.md:305-306`); mutation testing not re-run in this pass |
| 18 | Gates recorded without artifact (mix test, credo, compile, format, E2E 108/0) | UNCLEAR | per task scope, not to be run by this reviewer; noting per instruction #4 that these are stated claims without an attached CI/log artifact in the diff |
| 19 | Scope creep check | MET (none found) | Diff files (`CHANGELOG.md`, e2e spec, `cloudflare.ex`, `form.ex`, `form/video_drawer.ex`, `provider_client_test.exs`) all map 1:1 to 9B/9C/9D tasks; no unrelated changes observed |

**Summary**: 11 MET · 0 PARTIAL · 0 UNMET · 8 UNCLEAR

Notes:
- All spot-checked line citations (`mux.ex:545`, `bunny.ex:403`, `cloudflare.ex:273` region → actual `:268-284`, `plan.md` section G headings) resolve correctly against current file state — no stale citations found uncorrected.
- 9A bookkeeping is real and verified in `plan.md` itself (checkboxes and annotations agree).
- The UNCLEAR items are consistently the test-gate/mutation-testing claims (E2E RED/GREEN numbers, mix test/credo counts, empty-string rejection mechanics) — these require running the suite, which is out of this agent's scope per task instructions and belongs to a verification-runner agent.
