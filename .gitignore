/_build
/bower_components
/deps
/tmp
erl_crash.dump
*.ez
.DS_Store
brando.sublime-workspace
.sass-cache
node_modules
brando.sublime-project
excoveralls.post.json
doc
cover
npm-debug.log
/.elixir_ls
/.vscode
/priv/templates/brando.install/assets/backend/.yalc
/.playwright-mcp
/.pnpm-store/

# Claude Code working files. Plans, review artifacts and local settings stay
# on disk but out of git — they are session scratch, not project history.
# skills/ is the exception: AGENTS.md references those files directly.
.claude/
!.claude/skills/
!.claude/skills/**
