defmodule Brando.Trait.Revisioned do
  @moduledoc """
  Marks a Blueprint for immutable revision capture through `Brando.Revisions`.

  Prefer the equivalent `trait :revisioned` shorthand in Blueprint modules.
  """
  use Brando.Trait
end
