defmodule Brando.StatsChannelTest do
  use ExUnit.Case
  use Phoenix.ChannelTest
  # alias Brando.Integration.UserSocket

  @endpoint Brando.Integration.Endpoint

  test "stats channel join" do
    assert 1 == 1
  end
end